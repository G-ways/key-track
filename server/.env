# Mongo connection string
MONGO_URI=mongodb://localhost:27017/key_management

# Port the API listens on
PORT=4000

# Base URL of the deployed frontend. Used to build the URL encoded in the QR code.
FRONTEND_URL=http://localhost:5173

# How many hours a daily QR code stays valid (default 24)
QR_LIFETIME_HOURS=24
